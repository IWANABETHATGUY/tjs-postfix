let a = <td>
  <textarea
    cols={60}
    defaultValue={renderHTML(about, test, test + result + `${test.test.test + test}`)}
    name="about"
    onChange={onAboutChange}
    rows={5}
    style={{ fontSize: "-2" }}
    wrap="virtual"
  />
  <Link href="/formatdoc">
    <a tabIndex={-1} style={{ color: "#afafaf" + abdf }}>
      help
    </a>
  </Link>
</td>;
